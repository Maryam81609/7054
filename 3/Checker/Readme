To type-check the input file do the following:

- Change your working directory to Checker

# Compile all files (visitor, syntaxtree, symbol, error, parser), options in [] are optional
- ant [build] 


# Run checker for a given input file
- ant run -Dargs=PATH_RELATIVE_TO_BIN/INPUT_FILE

# Example
- assume Checker and checker_tests are in the same directory
- current working directory is Checker
- run the command
    * ant run -Dargs="../../checker_tests/arrays2.java"

